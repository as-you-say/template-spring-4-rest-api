<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "classpath:xsd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.template.api.board.mapper.BoardMapper">
    <select id="selectBoardList" parameterType="Board" resultType="Board"></select>
    <select id="selectBoardById" parameterType="Board" resultType="Board"></select>
    <insert id="insertBoard" parameterType="Board"></insert>
    <update id="updateBoard" parameterType="Board"></update>
    <delete id="deleteBoard" parameterType="Board"></delete>
</mapper>
