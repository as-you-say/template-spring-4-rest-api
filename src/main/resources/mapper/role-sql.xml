<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "classpath:xsd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.template.api.role.mapper.RoleMapper">
    <select id="selectRoleList" parameterType="Role" resultType="Role"></select>
    <select id="selectRoleById" parameterType="Role" resultType="Role"></select>
    <insert id="insertRole" parameterType="Role"></insert>
    <update id="updateRole" parameterType="Role"></update>
    <delete id="deleteRole" parameterType="Role"></delete>
</mapper>
