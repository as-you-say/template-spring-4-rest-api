<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "classpath:xsd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.template.api.account.mapper.AccountMapper">
    <select id="selectAccountList" parameterType="Account" resultType="Account"></select>
    <select id="selectAccountById" parameterType="Account" resultType="Account"></select>
    <select id="selectAccountByUsername" parameterType="Account" resultType="Account"></select>
    <insert id="insertAccount" parameterType="Account"></insert>
    <update id="updateAccount" parameterType="Account"></update>
    <delete id="deleteAccount" parameterType="Account"></delete>
</mapper>
